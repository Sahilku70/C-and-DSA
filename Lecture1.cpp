//First program......

#include<iostream>
/*
#include<iostream>
This line includes the input/output stream library in your program.
iostream stands for Input/Output Stream and is necessary to use cin (for input) and cout (for output).
The #include directive tells the compiler to include the contents of the file <iostream> before compiling.*/

using namespace std;      //C++ has different namespaces to avoid name conflicts.

int main(){
/*
int main()
This is the main function where the execution of the program starts.
int indicates the function returns an integer value (usually 0 if successful).
The {...} contains the body of the function.
*/
    cout<<"Hello World";
}
